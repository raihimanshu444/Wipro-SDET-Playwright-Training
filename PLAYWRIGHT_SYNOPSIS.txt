================================================================================
PLAYWRIGHT AUTOMATION SYNOPSIS (DAY 12 - DAY 14)
================================================================================

This document provides an easy-to-understand summary of the concepts, setup, 
and syntax used for UI Automation Testing using Playwright.

--------------------------------------------------------------------------------
DAY 12: INTRODUCTION TO PLAYWRIGHT & BASIC LOCATORS
--------------------------------------------------------------------------------

1. INITIAL SETUP & CONFIGURATION
   - Command: 'npm init playwright@latest' sets up the testing framework.
   - Configuration: 'playwright.config.js' controls which browsers to test on 
     (Chromium, WebKit, Firefox).
   - Tip: If Windows throws missing DLL errors (e.g., ext-ms-win-rtcore...) 
     for Firefox, you can safely comment out the Firefox project in the config 
     to continue practicing with Chromium.

2. CORE PLAYWRIGHT FUNCTIONS
   - test(): Defines a new test case.
   - expect(): Used to validate/check conditions (Assertions).
   - page: A "fixture" representing an isolated browser tab. 
     Always used inside an 'async' function because browser actions take time.
   - page.goto('url'): Tells the browser to open a specific website and wait 
     for it to load.

3. LOCATING ELEMENTS (Finding things on the screen)
   Concept: Before you can click or type, you must find the element.
   - page.locator(): The traditional way to find elements using XPath or CSS. 
     (e.g., page.locator('//*[@id="root"]...'))
   - page.getByRole(): The recommended, user-friendly way to find elements based 
     on what they are (button, link, heading).
   - .nth(index): Used when multiple elements match your search. 
     (e.g., .nth(1) selects the SECOND element found, as it starts at 0).

4. ACTIONS & ASSERTIONS (Doing things and checking results)
   - .click(): Simulates a mouse click on a located element.
   - expect(page.url()).toBe('url'): Checks if the browser navigated to the 
     correct page after clicking.

--------------------------------------------------------------------------------
KEY TAKEAWAYS (DAY 12)
--------------------------------------------------------------------------------
- Always use 'await' before page actions (like goto, click, or expect) because 
  the code needs to wait for the browser to finish the action.
- 'getByRole' is better than XPath because it's easier to read and works like a 
  real user looking at the screen.
- When a locator finds multiple identical buttons (like two "Request" links), 
  use '.nth()' to specify exactly which one you want to click.

--------------------------------------------------------------------------------
DAY 13: DATA-DRIVEN TESTING & WEB APP INTEGRATION
--------------------------------------------------------------------------------

1. INTEGRATING WITH A REAL WEB APP
   - Testing against a local server: Playwright can test apps running locally 
     (e.g., 'http://localhost:3000').
   - Prerequisites: The server must be running in a separate terminal before 
     starting the tests.

2. DATA-DRIVEN TESTING (DDT)
   - Concept: Running the same test logic multiple times with different inputs 
     (e.g., testing various login credentials).
   - Implementation: 
     - Store test data in an array of objects.
     - Use '.forEach()' to loop over the data and generate dynamic test cases.
   - Benefit: Reduces code duplication and ensures all edge cases (invalid pass, 
     short username, success) are tested.

3. ADVANCED ACTIONS & LOCATORS
   - .fill('value'): Used to type text into input fields like 'username' or 'password'.
   - ID Selectors: Using '#id' (e.g., '#username') to find elements precisely.
   - .textContent(): Capturing the text visible in an element (like an error message).

4. CODE REUSABILITY (UTILITIES)
   - Concept: Moving repetitive logic (like a login flow) into a separate 
     helper file ('utils.js').
   - Benefit: Makes the main test file cleaner and the logic reusable across 
     different test files. (First step towards Page Object Model).

--------------------------------------------------------------------------------
KEY TAKEAWAYS (DAY 13)
--------------------------------------------------------------------------------
- Data-Driven Testing makes your test suite highly efficient by separating 
  "what to test" (data) from "how to test" (logic).
- Modern web testing involves a loop: Enter Data -> Click -> Assert Message.
- Using a '.env' file is crucial for managing configuration like PORT numbers.
- Always check that your server is running before executing tests that target 
  localhost!

--------------------------------------------------------------------------------
DAY 14: ES MODULES & ADVANCED PLAYWRIGHT CONFIG
--------------------------------------------------------------------------------

1. ES MODULES (ESM) IN NODE.JS
   - .mjs Extension: Tells Node.js to use modern ECMAScript Modules.
   - import/export: Using 'import name from "./file.mjs"' and 'export default' 
     to share code between files instead of 'require' and 'module.exports'.
   - Modular Code: Separating math logic (op.mjs) from main logic (cal.mjs) 
     is a standard industry practice.

2. AUTOMATING THE TEST ENVIRONMENT (webServer)
   - Automation: You can configure Playwright to automatically start your 
     backend server (node index.js) before the tests begin.
   - Config Settings:
     - cwd: The directory where the server code lives.
     - command: The command to start the server.
     - url: The URL to wait for before running tests.
   - Benefit: No need to manually open two terminal windows anymore!

3. SCREENSHOT CONFIGURATION
   - Capture Logic: Controlled via 'playwright.config.js' under the 'use' block.
     - 'on': Take screenshots for every test.
     - 'only-on-failure': Only save screenshots when a test fails (Best practice).
     - 'off': Never take screenshots.
   - Storage: Screenshots are automatically saved in the 'test-results' folder.

4. BUILT-IN NODE MODULES
   - path Module: Used for directory management (path.resolve, path.join). 
     This ensures your code works correctly on both Windows and Linux.

--------------------------------------------------------------------------------
KEY TAKEAWAYS (DAY 14)
--------------------------------------------------------------------------------
- Using 'webServer' makes your automation truly independent and CI/CD ready.
- Modular code (mjs) makes your test scripts easier to maintain and read.
- 'screenshot: only-on-failure' is the most efficient way to debug without 
  cluttering your project with thousands of images.
- Always use 'path.resolve' for directory paths to avoid "File Not Found" errors.
